services:
  - type: web
    name: ai-video-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: REPLICATE_API_TOKEN
        sync: false
