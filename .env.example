# Local free model provider (recommended)
OLLAMA_MODEL=llama3.1:8b

# Optional: free cloud fallback
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free

# Existing video API token (optional if you don't use /generate)
REPLICATE_API_TOKEN=

PORT=3000
